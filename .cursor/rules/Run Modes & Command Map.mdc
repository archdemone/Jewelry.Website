---
alwaysApply: true
---

Memory 2 — Run Modes & Command Map (what to run):

Default server start: "start the server" = npm run dev:safe (baseline prod + sandbox dev)

Dev (live edit): npm run dev:safe (baseline prod on :3000 + sandbox dev on :3001).
Dev uses webpack (Turbopack off via NEXT_DISABLE_TURBOPACK=1).

Prod (parity/perf): npm run run:both (build + next start for both).

Quick commands:

Sandbox only: npm run dev:sandbox

Baseline only: npm run start:baseline

Stop servers: npm run stop

Status check: npm run status

Default if unclear: ask “dev or prod?”; if no answer → npm run dev:safe.